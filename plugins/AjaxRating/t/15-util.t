#!/usr/local/bin/perl

use Test::AjaxRating::Tools;
use Test::MT::Suite;
use DDP;
use AjaxRating::Util qw( get_config obj_to_type pluralize_type pluralize );

my $suite  = Test::MT::Suite->new();
my $app    = MT->instance;

subtest 'AjaxRating::Util::get_config' => sub {
    TODO: {
        local $TODO = "Not yet writen";
        fail('do it');
    }
};

subtest 'AjaxRating::Util::obj_to_type' => sub {
    TODO: {
        local $TODO = "Not yet writen";
        fail('do it');
    }
};

subtest 'AjaxRating::Util::pluralize_type' => sub {
    TODO: {
        local $TODO = "Not yet writen";
        fail('do it');
    }
};

subtest 'AjaxRating::Util::pluralize' => sub {
    TODO: {
        local $TODO = "Not yet writen";
        fail('do it');
    }
};

subtest 'AjaxRating::Util::reporter' => sub {
    TODO: {
        local $TODO = "Not yet writen";
        fail('do it');
    }
};

done_testing();

__END__
